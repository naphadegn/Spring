
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Gem@2021$
    url: jdbc:mysql://localhost:3306/mhadadb?serverTimezone=UTC&useUnicode=yes&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  batch:
    jdbc:
      initialize-schema: ALWAYS  
    
